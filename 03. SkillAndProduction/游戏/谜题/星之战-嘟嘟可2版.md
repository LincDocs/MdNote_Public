---
create_date: 2026-05-31
last_date: 2026-06-01
author:
  - LincZero
---
# 星之战-嘟嘟可2版 (草稿)

与第一部类似，但规则有一些变动。更难了许多。

图示: **RGB 表示不同颜色，且该色有两个嘟嘟可。rgb 表示不同颜色，且该色有一个嘟嘟可。x 表示不可能，o 表示在**

不过这个比较多要猜，就很难受了。

主要扫雷很好看出是不是要死猜（看出来要猜也等于解出来了）
而这种比较难判断出是不是需要开始猜了（等于没解出来）

## 同样适用的旧定式

- N色只占N行/列

## 定式 —— 无一雷

这种有点类似旧定式，指若某个地方是星，则剩余地方不可能再有星

与1不同，在前期比较难使用行列排除法

### 3格定式、6格定式 (最常用之一)

```
R x R
R x R
```

小于这个结构的变型也能用：如缺一格、如一条3格

```
R x _
R x 0
```

```
0 x 0
```

### 9格结构

```
R R R
R x R
R R R
```

缺几个结果一样

### 长条

```
x x x R R ... R R x x x

其中，数量 <= 5 时:

_ _ _ _ x _ x _ _ _ // 5
x x x R R R R R x x
_ _ _ _ x _ x _ _ _

_ _ _ x x x x _ _ _ // 4
x x x R R R R x x x
_ _ _ x x x x _ _ _

_ _ x x x x x _ _ // 3
x x x 0 x 0 x x x
_ _ x x x x x _ _
```

### 其他

- 行列余二法
- 行列已二法

### 单色类

```
x x
r r
x x 
```

## 定式 —— 无二雷 (双色)

指若某个地方是星，则剩余地方最多一星，不可能有两星。

无一雷定式是在自己中去除，这个是在别人颜色中去除。

快速掌握该定时的技巧：用一个 3x3 区域围绕目标颜色转一圈，看是去除该去除后是否剩余 2x2 区域

### 大L型

3+3/3+4

```
R
R x
R R R R R

  R
x R x
  R R R R
```

至于小L型用颜色拆分定式理解更简单

### T型

```
R 
R x
R R R
R x
R 
```

## 定式 —— 定双雷-冲突 (双色)

```
      x
      R
R R R R
R B R R
R R R R
      R

这里 B 若存在，则最右侧有三雷，冲突
```

### 3x4型

圆型/椭圆型

```
R R R R
R B B R
R R R R

可以允许缺失几个 R
此时通常可以排除 B 项
```

```
R R R R
R R B R
R R R R

这样也同理
```

### 4+1型

```
        R
x x x R R R R G x x x

则最上的 R 可以排除，若是则该行无法存在的两雷会紧贴
```

## 定式 —— 双色

```
xxx R R xxx G G xxx
    _ _ ... _ _
xxx R R xxx G G xxx

xxx R xxx G G xxx
    _ ... _ _
xxx R ... G G xxx
```

## 颜色拆分定式

关键是当一个连续色块去除了两个相近色块后，会变成一个无法容纳双色的情况

### 无法容纳双色的情况

2x2 四格占地的四种情况

```
R R
R R

R R
R

R
  R

R R
```

为了描述方便，后文将这种称为 "单色区"

### 拆分定式

只要一个颜色组有两个单色区组成，就可以认为这是颜色拆分定时

即假设 g 部分一个雷也没有，那么 r 部分就有两雷，但这并不可能，所以 g 部分至少有一雷。反之。

下面举例:

```
R R
  R R R

r r x x
x r g g
    x x
```

```
R
R R R R

r x x x
r r g g
    x x
```

```
_ _ _ _ R R R R _ _ _ _

        x x x x
x x x x r r g g x x x x
        x x x x
```

```
x R R
x R R R

x r x x
x r x g

// 之前的6格定式和其变形也可以用这个定式来解释，甚至更清晰
```

```
R R R _
R _ R R

r r g x
r x g g
```

```
R R R R
R R R R

r r g g
r r g g

// 不过该例子没什么意义，只要不是 4+4，有一边小于 4，都能提供到有效信息
```

## 分析法

### 横竖视角

### 拆分法

将一个颜色拆成两个颜色，那么就跟旧版本的一色一雷较像了 (一行/列二雷这个不像以外)

## 田字分析

### 非对称田

### 1111田

### 2222田

## 异色组合

多种颜色视为一个颜色整体去分析

## 强弱链

## 猜星技巧

由于星总是稀少的

```
a a
A A
B B

这里如果 a+A 区域和 A+b 区域，都确定有一个星。
那么有两种可能：

一是 a 和 B 分别有一星。此时局部星率 2/6 = 0.33%
二是 A 有一星，a 和 B 没有星。此时局部星率 1/6 = 16.6%

而每行/列有1星的情况下，整体星率分别为 1/9 = 11.1%
而每行/列有2星的情况下，整体星率分别为 2/9 = 22.2%

看起来都是倾向于共占星更接近整体星率？

但感觉这算法不对，局部星率和整体星率的练习不像扫雷中使用雷率那么固定，而且扫雷雷率在残局中的可靠性也不高。而且这里两个概率相差也不够高。

感觉很难猜，很难有高正确率的猜法
```

```
4+1 型

  R
R R R R

  R
R R ? ? // 似乎 4+1 的这个位置可以猜一下？收益也较高
```

## 标记法？

a的和为1，b的和为2？

```
1 1 1 2 2 2 2 2 3
1 p 1 1 2 2 2 3 3
p p p p b 2 3 3 3
z z p p b b 3 3 3
z p p p b b 3 r r
z p p b b b b r r
z p p b b b b y r
h h h h h y y y r
h h h h h y y r r
```