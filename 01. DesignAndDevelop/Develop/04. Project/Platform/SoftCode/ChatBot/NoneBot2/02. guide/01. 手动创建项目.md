# 手动创建项目

仅用于了解原理，不建议手动创建，建议使用脚手架 nb-cli 创建

https://nonebot.dev/docs/tutorial/application

话说手动多了一些文件是脚手架里看不到的，如入口文件 bot.py

> [!tip]
> 
> 如果你使用 `nb-cli` 创建项目，入口文件不会被创建，该文件功能会被 `nb run` 命令代替。
>
> 运行脚本也由 `python bot.py` 换成了 `nb run`




